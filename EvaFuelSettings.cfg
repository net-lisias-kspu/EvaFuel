EvaFuelSettings
{
	// WARNING: Check Squad/Parts/Prebuilt/kerbalEVA.cfg before modifiying these values.
	
	// The maximum amount of fuel that will be transfered between ship and EVA.
	EvaTankFuelMax = 5
	
	// The name of the transfered fuel on the EVA side.
	EvaPropellantName = EVA Propellant
	
	// The name of the transfered fuel on the ship side.
	ShipPropellantName = MonoPropellant
	
	// The name of the electric resource, used to check rescue contract ship.
	ShipElectricityName = ElectricCharge
	
	//Each 1 unit of ship fuel will become this many units of Eva Fuel. Decimals are allowed.
	FuelConversionFactor = 1.0
}
